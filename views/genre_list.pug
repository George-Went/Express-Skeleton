extends layout

block content
  h1= title
  
  ul
    each genre in genre_list
      li 
        a(href=genre.url) #{genre.name} 
        //|  (#{author.date_of_birth_format} - #{author.date_of_death_format})

    else
      li There are no genres.